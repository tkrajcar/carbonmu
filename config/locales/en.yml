en:
  emits:
    say: '%{name} says, "%{message}"'
    pose: '%{name} %{message}'
    semipose: '%{name}%{message}'
    emit: '<%{name}> %{message}'

  location:
    name: '%{message}'
    description: '%{message}'
